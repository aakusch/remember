{
  "name": "@remember/viewer",
  "version": "0.0.1",
  "description": "Browser viewer + admin UI for the remember wiki platform (Astro + React islands)",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'viewer: Astro scaffold lands in implementation phase. Run `npm create astro@latest` here when ready.'",
    "build": "echo 'viewer: placeholder build (no-op in scaffold)'",
    "test": "echo 'viewer: no tests yet'",
    "typecheck": "echo 'viewer: placeholder typecheck'",
    "lint": "echo 'viewer: no lint yet'"
  },
  "engines": {
    "node": ">=20"
  }
}
