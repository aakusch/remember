{
  "name": "@useremember/viewer",
  "version": "0.0.1",
  "description": "Browser viewer + admin UI for the remember wiki platform (Astro)",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "astro check",
    "lint": "echo '(eslint not yet configured)'"
  },
  "dependencies": {
    "@astrojs/node": "^9.0.0",
    "@useremember/core": "workspace:*",
    "astro": "^5.0.0",
    "marked": "^14.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
